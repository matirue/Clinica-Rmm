import { VerifyCaptchaDirective } from './verify-captcha.directive';

describe('VerifyCaptchaDirective', () => {
  it('should create an instance', () => {
    const directive = new VerifyCaptchaDirective();
    expect(directive).toBeTruthy();
  });
});
